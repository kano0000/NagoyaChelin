<div class = "container mt-5">
  <div class = "row">
    <div class = "col-8 mx-auto">
      <h1>followers</h1>
      <% if @users.present? %>
        <%= render "public/users/index", users: @users %>
      <% else %>
        <p class="text-center">You don't have any followers yet.</p>
      <% end %>
    </div>
  </div>
</div>