<header>
  <nav class="navbar navbar-expand-lg navbar-dark bg-danger p-4">
    <div class="container">
      <a class="navbar-brand" href="/"><span><%= image_tag "logo.png", :size => "80x80", class:"img-fluid" %> なごやしゅらん</span></a>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNavDropdown">
        <ul class="navbar-nav ml-auto">
        <% if admin_signed_in? %>
          <li>
            <%= link_to admin_users_path, class: 'nav-link text-light' do %>
              <i class="fas fa-home"></i> Users
            <% end %>
          </li>
          <li>
            <%=  link_to stores_path ,class: 'nav-link text-light' do %>
              <i class="fa-solid fa-utensils"></i> Stores
            <% end %>
          </li>
          <li>
            <%= link_to destroy_admin_session_path, method: :delete,class: 'nav-link text-light' do %>
              <i class="fas fa-sign-out-alt"></i> Log out
            <% end %>
          </li>
        <% end %>
        </ul>
      </div>
    </div>
  </nav>
</header>